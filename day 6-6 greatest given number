
#include <stdio.h>

int main()
{
    int arr[]={10,45,32,67,23,89,5};
    int n=sizeof(arr)/sizeof(arr[0]);
    int m=arr[0];
    for(int i=1;i<n;i++)
{
    if(arr[i]/m)
    {
        m=arr[i];
    }
}
printf("%d",m);
    return 0;
}
